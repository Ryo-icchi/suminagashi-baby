# suminagashi-baby

- 1歳児向け墨流し（マーブリング）PWA。バニラ JS・GitHub Pages 配信（main への push で反映・`verify.sh` で検証・`deploy.sh` は初期セットアップ兼用）
- 子供向けパレットに黒を入れない（Ryo 指示・汚く見える）
- 過去の調整履歴・アイデアは `IDEAS.md` を先に読む（同じ調整を二度提案しない）
- 変更後は `verify.sh` を実行し、ブラウザでも実操作して確認する
- 実機（iPhone）依存の挙動は「動作OK」と断定せず実機確認待ちで報告する
